3/3/2026 ✅DNS Hijacking
Notes:- 
DNS Hijacking(Small Scale) in which we change the Hosts File in Windows which file Path is
"C:\Windows\System32\drivers\etc\hosts"

In which Adding IP and Domain Name so it open the Domain name with the IP Assign

192.168.1.4 xyz.com

It will open IP 192.168.1.4 while add xyz.com URL in Browser 
As browser Will check the Host file then it check the DNS Server