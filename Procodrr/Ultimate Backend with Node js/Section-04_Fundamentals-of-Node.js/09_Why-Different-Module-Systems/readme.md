Node js created in 2009 since to create complex application developer created there own module system so there is commonjs module system that is very popular it's name given as it will use in whole javascript eco-system in frontend and backend so node js impement it but in 2015 the javascript have created there own module system that is ES6 it is very powerfull then commonjs because it comes with pre enable "use strict" mode all variables are created in module scope and for client side html the javascript have defer link 
